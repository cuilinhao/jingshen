# 原图的自动深度参考验证（不是 App 运行截图）

输入是用户提供的 yuntu0920.png，1060×1410，RGBA 的 alpha 均为 255。使用随工程的完整模型真实权重，无人工标注。

CPU 参考工具解释该模型的 MLProgram，2459 个操作均完成，检查所有输出形状和有限性、权重元数据及边界。它使用 PyTorch 的数值算子、模拟 FP16 边界；不是 Apple Core ML，不能证明 iOS 适配和画质已经通过。

`depth.png`：参考输出经生产 Swift 的 p1/p99 归一化得到的相对深度（亮近暗远）。`near-focus-blur.png`、`far-focus-blur.png`：生产 Swift `ContinuousFocusMasks` 的实际结果（黑色0清晰、白色255虚化）。图像均缩放到530×705供阅读。`original.jpg` 是缩小的原图，仅用于观察区域。

| 区域采样点 | 相对深度 | 点瓶子后的 blur | 点柜子后的 blur |
|---|---:|---:|---:|
| 显示器 (0.15, 0.52) | 0.6766772 | 0 | 237 |
| 瓶身 (0.484536, 0.785438) | 0.8525564 | 0 | 255 |
| 黄色玩偶 (0.21, 0.90) | 0.97103786 | 0 | 255 |
| 柜门 (0.839347, 0.548325) | 0.2730504 | 255 | 0 |

之前日志的8个位置全部取得有效深度：柜子三点约0.22–0.27，瓶子五点约0.85–0.87。精确坐标/数值见 `Verification/v4/actual-original-taps.log`。这里的0–1不是米数；0–255是虚化控制蒙版，不是渲染后图像清晰度评分。

参考预测仅置于 Tests/Fixtures，不复制到 App。普通导入 / 内置原图在 iPhone 上仍必须调用真正的 Core ML 计算；未通过时测试会失败，不使用本目录结果遮掩。
