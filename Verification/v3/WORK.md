# v3 工作记录
需求：同一已确认景深层一起清晰；未知区域不等同背景；保留录屏 UI、纯 Swift、无外部模型下载。
已批准范围：修复选取与同景深分层；普通照片缺少远近信息时提供可校正分层，不宣称自动深度估计。
1. 重现旧逻辑，写回归测试（unknown 不得当 far）。
2. 景深分层（near/middle/far/unknown）、绘制/主体赋层/多边形/撤销；统一按层生成蒙版。
3. 导入、点击、缓存、草稿 v3、导出路径接入；编辑蒙版提交更换照片会话 ID。
4. 保留主界面；新增分层校正面板。无法命中已标记层时明确提示，不伪移动对焦框。
5. 原图 yuntu0920.png + 明确标为人工的测试分层，用于本场景近远切换回归；不为任意导入图套用样例。
6. 核心测试、源码 parse、工程检查、资源和 zip 检查；不能执行 Xcode 或真机测试。
基线：35 Swift 测试通过。无 xcodebuild。所有改动仅限新解压的隔离目录，保留用户旧 ZIP。

执行结果：旧版回归断言实际失败后，完成分层与 unknown 策略；已去除 sharpMask 单主体清晰逻辑。
已实现校正工具、同层保护、缓存失效、草稿v3迁移、显式人工样例、文案及验证资料。
过程中一个新增测试表达式触发 Swift 类型推断超时，拆成显式 Int 中间值后原测试语义保持不变。
最终检查以 core-final.log / project-final.log / project-validation.log / swift-parse.log 为准；不声称运行过 Xcode。
